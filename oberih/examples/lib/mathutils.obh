struct Point {
    x: Number,
    y: Number
}

fn addPoints(a: Point, b: Point) -> Point {
    return Point(a.x + b.x, a.y + b.y)
}

fn sumList(arr: List<Number>) -> Number {
    let total = 0
    for (n in arr) {
        total = total + n
    }
    return total
}
